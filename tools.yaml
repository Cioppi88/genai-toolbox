sources:
  bigquery-source:
    kind: bigquery
    project: "${BQ_PROJECT_ID}"

tools:
  execute-sql:
    kind: bigquery-execute-sql
    source: bigquery-source
    description: Esegue query SQL contro BigQuery

toolsets:
  bigquery-tools:
    - execute-sql
