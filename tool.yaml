sources:
  my_db:
    kind: bigquery
    location: EU
    project: hip-nuova-folati

tools:
  list_tables:
    kind: bigquery-list-table-ids
    source: my_db
    description: Elenca tutte le tabelle nel progetto BigQuery

  get_table_info:
    kind: bigquery-get-table-info
    source: my_db
    description: Restituisce lo schema di una tabella

  exec_sql:
    kind: bigquery-execute-sql
    source: my_db
    description: Esegue una query SQL arbitraria

toolsets:
  chat-with-db:
    - list_tables
    - get_table_info
    - exec_sql
